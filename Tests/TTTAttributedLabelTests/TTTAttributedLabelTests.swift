import XCTest
@testable import TTTAttributedLabel

final class TTTAttributedLabelTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TTTAttributedLabel().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
